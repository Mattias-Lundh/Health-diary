import { writable } from "svelte/store";

export const selectedCampaign = writable(undefined);
export const consumptions = writable(undefined);
export const consumptionTemplates = writable(undefined);
export const meals = writable(undefined);
export const excersises = writable(undefined);
export const excersiseTemplates = writable(undefined);
export const campaigns = writable(undefined);
export const authToken = writable(undefined);
